## 2026-04-12 12:00 — Game Class Initialization

- Implemented the core `Game` class in `game/game.py` to manage game state initialization, piece spawning, and current cell retrieval.
- The class correctly initializes the board, sets score/frame count, and calls `spawn_piece()`.
- Tested with `python -m py_compile game/game.py`, which passed successfully.

**Next step:** Proceed with the next piece of functionality.

---